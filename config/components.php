<?php

return [
    /*
     * Shoe component/shape names captured at the pre_assembly checkpoint.
     * Placeholder values until the real component set is finalized — rename
     * these (and the matching COMPONENT_TYPES list in the desktop capture
     * script) once known. Not used at the preparation checkpoint, where every
     * piece is the same shape.
     */
    'types' => [
        'component_a',
        'component_b',
        'component_c',
        'component_d',
    ],
];
